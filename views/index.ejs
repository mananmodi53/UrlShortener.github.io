<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>Document</title>
    <style>
        .yhyh:hover{
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <div class="container">
    <h1>URL Shrinker</h1>
    <form action="/shortUrls" method="POST" class="my-4 form-inline" >
        <label for="fullUrl" class="sr-only">Url</label>
        <input required type="url" name="fullUrl" id="fullUrl" class="form-control col mr-2" autocomplete="on" >
        <br>
        <div class="text-center">
        <button class="btn btn-success rounded-pill yhyh" type="submit">Shorten</button>
    </div>
    </form>

    <table class="table table-striped table responsive">
        <thead>
            <tr>
                <th>Full Url</th>
                <th>Short Url</th>
                <th>Clicks</th>
            </tr>
        </thead>
        <tbody>
            <% shortUrls?.forEach(shortUrl=>{ %>
                <tr>
                    <td><a href="<%= shortUrl?.full %>" target="_blank"><%= shortUrl?.full %></a></td>
                    <td><a href="<%= shortUrl?.short %>" target="_blank"><%= shortUrl?.short %></a></td>
                    <td><%= shortUrl?.clicks %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
</body>
</html>